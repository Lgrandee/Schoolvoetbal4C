
<Window
    x:Class="school.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:school"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"

    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <TabView HorizontalAlignment="Center" VerticalAlignment="Center" Width="400">
            <!-- Tab voor Inloggen -->
            <TabViewItem Header="Inloggen">
                <StackPanel Spacing="20" Padding="10">
                    <TextBox x:ConnectionId='10' x:Name="LoginUsernameTextBox" Header="Gebruikersnaam" PlaceholderText="Voer je gebruikersnaam in" />
                    <PasswordBox x:ConnectionId='11' x:Name="LoginPasswordBox" Header="Wachtwoord" PlaceholderText="Voer je wachtwoord in" />
                    <TextBlock x:ConnectionId='12' x:Name="LoginErrorTextBlock" Visibility="Collapsed" />
                    <Button x:ConnectionId='13' Content="Inloggen"                           />
                </StackPanel>
            </TabViewItem>

            <!-- Tab voor Registreren -->
            <TabViewItem Header="Registreren">
                <StackPanel Spacing="20" Padding="10">
                    <TextBox x:ConnectionId='5' x:Name="RegisterUsernameTextBox" Header="Gebruikersnaam" PlaceholderText="Voer een gebruikersnaam in" />
                    <PasswordBox x:ConnectionId='6' x:Name="RegisterPasswordBox" Header="Wachtwoord" PlaceholderText="Voer een wachtwoord in" />
                    <PasswordBox x:ConnectionId='7' x:Name="ConfirmRegisterPasswordBox" Header="Bevestig Wachtwoord" PlaceholderText="Bevestig je wachtwoord" />
                    <TextBlock x:ConnectionId='8' x:Name="RegisterErrorTextBlock" Visibility="Collapsed" />
                    <Button x:ConnectionId='9' Content="Registreer"                              />
                </StackPanel>
            </TabViewItem>
        </TabView>


        <StackPanel>
            <Button x:ConnectionId='2' x:Name="FetchTeamsButton" Content="Load Teams"                                />
            <ListView x:ConnectionId='3' x:Name="TeamsListView">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <Image Source="{Binding LogoUrl}" Width="50" Height="50" Margin="5" />
                            <TextBlock Text="{Binding Name}" FontSize="16" VerticalAlignment="Center" Margin="10,0,0,0" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>

    </Grid>


</Window>

